{{% BLOCKS }}
<!DOCTYPE html>
<html lang="en">
<html>
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{{$ title }}Memory{{/ title }}</title>
        {{$ stylesheets }}
            <link rel="stylesheet" href="styles/home.css">
            <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
        {{/ stylesheets }}
    </head>
    <body>
    <div class="container">
        <h4>{{{ intro }}}</h4>
    </div>

    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="column">
                    <h5>{{{ calculate_memory }}}</h5>
                    <img src="/mod/simulation/pix/calculo.png" alt="calculo" class="img-fluid">
                    <a href="{{{ calculate_url }}}">{{{ explore }}}</a>
                </div>
            </div>

            <div class="col-md-6">
                <div class="column">
                    <h5>{{{ simular_reemplazo }}}</h5>
                    <img src="/mod/simulation/pix/reemplazo.png" alt="reemplazo" class="img-fluid">
                    <a href="{{{ replace_url }}}">{{{ explore }}}</a>
                </div>
            </div>
        </div>
    </div>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
        {{$ scripts }}
            <script src="scripts/script.js"></script>
        {{/ scripts }}
    </body>
</html>
